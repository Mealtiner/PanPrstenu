---
/**
 * Příběh letošního ročníku
 * URL: /cs/pribeh/, /en/pribeh/, /de/pribeh/, /sk/pribeh/, /uk/pribeh/
 * Datum: 2026-05-03
 *
 * Placeholder — finální text dorazí od dramaturgie.
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import Breadcrumb from '@components/ui/Breadcrumb.astro';
import Quote from '@components/blocks/Quote.astro';
import AlertBox from '@components/blocks/AlertBox.astro';
import Divider from '@components/decorative/Divider.astro';
import Button from '@components/ui/Button.astro';
import type { Lang } from '@i18n/ui';
import { getTranslation } from '@i18n/ui';
import { getPageContent } from '@lib/content/pages';

export async function getStaticPaths() {
  return [
    { params: { lang: 'cs' } },
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
    { params: { lang: 'sk' } },
    { params: { lang: 'uk' } },
  ];
}

const { lang } = Astro.params as { lang: Lang };
const t = getTranslation(lang);
const tp = getPageContent('pribeh', lang);

const breadcrumbItems = [
  { label: t('nav.home'), href: `/${lang}/` },
  { label: t('nav.story') },
];
---

<BaseLayout
  title={t('nav.story')}
  description={tp('description')}
  lang={lang}
>

  <section class="py-12 bg-[var(--color-bg-darkest)] border-b border-[var(--color-gold-darkest)]">
    <div class="container-base">
      <Breadcrumb items={breadcrumbItems} />
      <h1 class="font-[family-name:var(--font-display)] text-4xl md:text-5xl uppercase tracking-wider text-[var(--color-gold-light)] mt-4">
        {t('nav.story')}
      </h1>
      <p class="font-[family-name:var(--font-serif)] italic text-lg text-[var(--color-text-on-dark-muted)] mt-2 max-w-2xl">
        {tp('subtitle')}
      </p>
    </div>
  </section>

  <section class="py-16">
    <article class="container-base max-w-3xl">

      <Quote author="J. R. R. Tolkien" source={tp('quote.source')} tone="dark">
        {tp('quote.text')}
      </Quote>

      <p class="text-lg text-[var(--color-text-on-dark)] leading-relaxed mb-6">
        {tp('intro_p1')}
      </p>

      <p class="text-base text-[var(--color-text-on-dark)] leading-relaxed mb-6">
        {tp('intro_p2_pre')}<a href={`/${lang}/novinky/`} class="underline text-[var(--color-gold-light)]">{tp('intro_p2_link')}</a>{tp('intro_p2_post')}
      </p>

      <Divider variant="line-diamond" tone="dark" class="my-10" />

      <AlertBox variant="info" title={tp('alert.title')}>
        {tp('alert.body_pre')}<a href={`/${lang}/frakce/`} class="underline">{tp('alert.body_link_factions')}</a>{tp('alert.body_sep1')}<a href={`/${lang}/pravidla/`} class="underline">{tp('alert.body_link_rules')}</a>{tp('alert.body_sep2')}<a href={`/${lang}/svet-stredozeme/`} class="underline">{tp('alert.body_link_world')}</a>{tp('alert.body_sep3')}<a href={`/${lang}/prakticke-info/`} class="underline">{tp('alert.body_link_practical')}</a>{tp('alert.body_post')}
      </AlertBox>

      <div class="mt-10 flex flex-wrap gap-3">
        <Button href={`/${lang}/frakce/`} variant="primary" size="md" icon="lucide:arrow-right">
          {t('cta.show_all_factions')}
        </Button>
        <Button href={`/${lang}/svet-stredozeme/`} variant="secondary" size="md">
          {t('cta.world_of_middle_earth')}
        </Button>
        <Button href={`/${lang}/novinky/`} variant="ghost" size="md">
          {t('nav.news')}
        </Button>
      </div>

    </article>
  </section>

</BaseLayout>
