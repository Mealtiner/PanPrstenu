# Dependencies
node_modules/

# Build output
dist/
.astro/

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Cache
.cache/
.eslintcache
.parcel-cache/
.npm
.yarn

# CLAUDE.md §9 — deploy targety jsou na FTP serveru, ne v repu.
# Workflowy nahrávají přímo z dist/. Pokud někdo lokálně omylem
# vytvoří web/, zůstane mimo git.
web/

# Astro types
.astro

# Lokální pracovní podklady (audit balíky, .docx, scratch). Neveřejné, nedeployovat.
průběžný postup nasazování/

# Pracovní zdrojové soubory grafiky (.ai, .psd, .docx) — jen v lokálu,
# výstupy jsou v public/images/. Repo neobsahuje obrovské binárky.
grafika/

# Editor noise
.DS_Store
**/.DS_Store
_local-edits/

# Lokální kopie dist/ pro manuální FTP upload (mimo CI deploy)
public_html/
public_html_new/

# Lokální pracovní podklady — PSI/Lighthouse reporty, návody, scratch.
Analyzy/
Navody/

# Lokální kopie kódu registracky (includes/db_init.php obsahuje
# plaintext DB credentials). NIKDY tuto složku necommituj.
Registračka/
Registracka/

# Claude Code lokální stav (lock pro /loop a /schedule). settings.json tracknutý zůstává.
.claude/scheduled_tasks.lock

# Generated build artifacty — regenerují se z `npm run build` (prebuild hook).
# llms.txt zůstává tracknutý (12 kB rozcestník), full text se generuje vždy znovu.
public/llms-full.txt
